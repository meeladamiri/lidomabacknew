# لیدوماتریپ — بک‌اند

Express + TypeScript + Prisma + PostgreSQL. جایگزین بک‌اند قدیمی Odoo.
فرانت در مخزن جداگانه‌ی `../front` است.

**قبل از هر کاری `../front/docs/PROGRESS.md` را بخوان** — منبع حقیقت پروژه
آنجاست، نه اینجا. باگ‌های باز: `../front/docs/OPEN-BUGS.md`.
دیپلوی: `../front/docs/DEPLOY.md`.

---

## دیتابیس — این‌ها قابل مذاکره نیستند

**دیتابیس production است.** ۲۸۶٬۵۲۷ کاربر، ۲۹٬۶۵۹ رزرو، داده‌ی واقعی مشتری.

**هیچ دستور نوشتن یا حذف بدون اجازه‌ی صریح کاربر.** شامل `UPDATE`، `DELETE`،
`prisma migrate reset`، و اسکریپت‌های اصلاح داده. خواندن آزاد است.

**به `--shadow-database-url` هرگز دیتابیسی که داده دارد نده.** Prisma آن را
drop می‌کند. از `--from-empty` یا `--from-schema-datasource` استفاده کن.

مهاجرت با `npx prisma migrate diff` ساخته و بررسی می‌شود، و فقط با تایید
کاربر اجرا. مهاجرت‌های افزودنی (ستون nullable، جدول تازه) بی‌خطرند؛
تغییر نوع و حذف ستون نه.

> ⚠️ `Review.createdAt` مثل خیلی از فیلدهای دیگر `@map` ندارد. اسم ستون را از
> `schema.prisma` بردار، حدس نزن.

---

## تله‌های تکرارشونده

**`publicId` با id داخلی فرق دارد.** ۱٬۶۴۰ کد اودو (`ODOO-<id>`) همزمان id
داخلیِ اقامتگاه *دیگری* هستند. resolve فقط در یک مسیر انجام می‌شود
(`resolveAdminResidenceId`) — به همه‌ی endpointها پخشش نکن، وگرنه تله به داخل
API منتقل می‌شود جایی که caller نمی‌تواند بگوید کدام عدد را منظور داشته.

**`include` هر ستون را می‌فرستد.** ستون تازه همان لحظه‌ای که اضافه شود عمومی
می‌شود. برای payload عمومی `select` صریح بنویس یا فیلد داخلی را destructure کن.
(یک بار یادداشت عملیاتی غیرفعال‌سازی روی صفحه‌ی عمومی نشت کرد.)

**`select` با `as const` تایپ‌چک نمی‌شود.** فیلد اشتباه فقط در زمان اجرا
می‌ترکد — همان چیزی که `GET /api/users/me` را برای هر کاربر لاگین‌شده ۵۰۰ کرد.

**پارس اعداد فارسی:** `\d` فقط ASCII را می‌گیرد. برای اعشار `parseCoord` نه
`parseNum` — دومی `35.6892` را به `356892` تبدیل می‌کند.

**زمان تهران UTC+3:30 است و DST ندارد** — برای باکت‌بندی روزانه.

---

## دیپلوی

اپ لیارا: `lidoma` (platform: node). از این پوشه `liara deploy`.
**اول بک‌اند تا آخر، بعد فرانت** — بک‌اندی که وسط push قطع شود، فرانت جدید را
روی بک‌اند قدیمی می‌گذارد.

دیپلوی فقط وقتی کاربر بخواهد، و همه‌ی تغییرات یکجا.

`.env` و `.env.liara` در گیت نیستند. رمز دیتابیس و Redis هرگز وارد گیت نشود.

---

## سبک کار

گزارش پایان نوبت کوتاه. توکن‌محتاط کار کن. بعد از هر تغییر `PROGRESS.md`،
هر دو مخزن commit و push شوند.
