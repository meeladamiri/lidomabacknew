node_modules/
dist/
.env
.env.local
.env.liara
*.log
.DS_Store
src/generated/
tsconfig.tsbuildinfo

# Generated by scripts/upload-images.js (on the Odoo source server) — a
# store_fname -> Liara URL map consumed by migrate-odoo-residences.ts.
# Regenerated per migration run, not source, and ~15MB — don't commit it.
scripts/image-map.json
